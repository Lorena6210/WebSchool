// Adicione outras funções auxiliares aqui se necessário
export const validateRA = (RA: string) => /^RA\d+$/i.test(RA);

// Funções para API (exemplo, adapte conforme necessário)
// export const fetchUsuarios = async () => { ... };
// export const fetchTurmasLista = async (idAluno: number, ... ) => { ... };